name: Deploy naar GitHub Pages

# Bouwt het spel en zet het online zodra er naar master wordt gepusht.
# De speelbare link verschijnt daarna op:
#   https://camiel20.github.io/ruimte-spellen-pro/

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          # Zelfde Node als lokaal (npm 11): npm 10 struikelt over het
          # lockfile-formaat van npm 11 (optionele platform-varianten).
          node-version: 24
      - run: npm ci
      # Test-gate: de validator (die onspeelbare levels vangt) + de smoke-tests
      # draaien hier, zodat een kapot level nooit stil live kan gaan.
      - run: npm test
      - run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
